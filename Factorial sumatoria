#include ciostream>
using namespice std;
int main () 
int i, N, signo = 1, f = 1;
float X, acum = 1;
cout << "Ingresar el numero de elementos:
cin >> N;
} while (N < 0);
do {
cout << "Ingresar el valor de la serie X: =.
cin >> X;
} while (X < 0);
for (i = 1; i <= N; i++)
асит = acum
+ signo * pow(X, j) / f;
‡ = + * i;
signo = -signo;
cout «« "La sumatoria es:
acum;
return 0;
